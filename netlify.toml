[build]
  base = "frontend/code-companion-main"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

# Optional: if you have client-side routing
#[[redirects]]
#  from = "/*"
#  to = "/index.html"
#  status = 200
